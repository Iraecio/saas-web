import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { CurrencyPipe, DatePipe } from '@angular/common'; import { ResellerCreditService } from '../../services/reseller-credit'; import { CreditSale, PaginatedResult } from '../../../../core/models/reseller-credit.model';
@Component({selector:'app-credit-sales',changeDetection:ChangeDetectionStrategy.OnPush,imports:[FormsModule,CurrencyPipe,DatePipe],template:`<div class="p-6 space-y-5"><h1 class="text-3xl font-bold">Vendas e margem</h1><div class="grid gap-2 sm:grid-cols-3"><input class="form-input" placeholder="UUID do cliente" [(ngModel)]="clientId"><input class="form-input" type="number" min="1" [(ngModel)]="amount"><button class="btn-primary" (click)="sell()">Vender</button></div>@if(error()){<p class="text-red-600">{{error()}}</p>}<div class="card"><table class="w-full"><tbody>@for(x of sales();track x.id){<tr><td class="p-3">{{x.clientId}}</td><td>{{x.creditAmount}}</td><td>{{x.totalSaleCents/100|currency:'BRL'}}</td><td>{{x.marginCents/100|currency:'BRL'}}</td><td>{{x.createdAt|date:'shortDate'}}</td></tr>}</tbody></table></div></div>`}) export class CreditSalesPage{private s=inject(ResellerCreditService);readonly sales=signal<CreditSale[]>([]);readonly error=signal('');clientId='';amount=1;constructor(){this.load()}load(){this.s.listSales().subscribe({next:r=>this.sales.set(Array.isArray(r)?r:((r as PaginatedResult<CreditSale>).data??(r as PaginatedResult<CreditSale>).items??[])),error:e=>this.error.set(e.message)})}sell(){this.s.sell({clientId:this.clientId,creditAmount:this.amount}).subscribe({next:()=>this.load(),error:e=>this.error.set(e.message)})}}
