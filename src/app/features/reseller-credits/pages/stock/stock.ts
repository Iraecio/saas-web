import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core'; import { CurrencyPipe, DatePipe } from '@angular/common'; import { ResellerCreditService } from '../../services/reseller-credit'; import { StockSummary } from '../../../../core/models/reseller-credit.model';
@Component({selector:'app-credit-stock',changeDetection:ChangeDetectionStrategy.OnPush,imports:[CurrencyPipe,DatePipe],template:`<div class="p-6 space-y-5"><h1 class="text-3xl font-bold">Estoque</h1>@if(stock();as s){<p class="text-4xl font-bold">{{s.totalAvailable}} <small class="text-base">créditos disponíveis</small></p><div class="card"><table class="w-full"><tbody>@for(l of s.lots;track l.id){<tr><td class="p-3">{{l.sourceType}}</td><td>{{l.remainingCredits}} / {{l.initialCredits}}</td><td>{{l.unitCostCents/100|currency:'BRL'}}</td><td>{{l.createdAt|date:'shortDate'}}</td></tr>}</tbody></table></div>}@if(error()){<p class="text-red-600">{{error()}}</p>}</div>`}) export class CreditStockPage{private s=inject(ResellerCreditService);readonly stock=signal<StockSummary|null>(null);readonly error=signal('');constructor(){this.s.getStock().subscribe({next:x=>this.stock.set(x),error:e=>this.error.set(e.message)})}}
